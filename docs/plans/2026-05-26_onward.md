# Development Plan: 2026-05-26+

**Author:** DeepSeek | **Status:** Draft for review

## Where we are

- 146 tests passing, all features stable
- Evidence audit loop closed (claim → verify/fail → autonomous verifier)
- TUI has tab bar, scoped chat, slash commands, token/cost display
- DESIGN_SYSTEM.md codifies all visual rules
- Claude is exploring other project areas

## Immediate: P1 (next session)

### 1. Monitor Tab — Real Dashboard (~2-3h)
Per DESIGN_SYSTEM.md §8.3. Currently shows Chat placeholder.

```
Glyph  Agent      Status   Latency   Tok In   Tok Out   Cost
●      claude     idle     2.1s      22k      847       $0.12
⠋      gemini     thinking 12s…     8.3k     1.2k      $0.04
○      openclaw   unknown  —         —        —         —
```

Implementation: reuse existing `AgentActivity` data from the agents sidebar.
Format with `format_latency` / `format_tokens`. Add cost column from pricing.

### 2. Footer Upgrade (~1h)
Per DESIGN_SYSTEM.md §10 — 3-segment structure:
```
[Tab switch] [Space select] [Enter send]  │  You → claude  │  v0.1.0
```
- Left: keyboard hints (already done)
- Center: active target (agent name or "N selected")
- Right: version string

---

## Short-term: P2 (1-2 sessions)

### 3. State-Machine Workflows (Maestro Task 3) (~3-4h)
Extend YAML workflow format with per-step state:
- `pending → claimed → in_progress → review → verified → done`
- Orchestrator manages transitions
- TUI Workflow panel shows timeline (glyph + status per step)

### 4. Agent Principles (Maestro Task 4) (~1-2h)
Manifest extension: `principles = ["always_write_tests_first"]`
Orchestrator attaches to task context. Advisory initially.

### 5. Help Modal (`?`) (~1-2h)
Per DESIGN_SYSTEM.md §11.2:
- Centered overlay, 60%×60%
- Shows all shortcuts + slash commands
- `?` or `Esc` to close

### 6. Rich Text Input (~2-3h)
Multi-line input with cursor movement (tui-textarea or custom).
Listed in April brainstorm, Phase 5 ROADMAP.

---

## Medium-term: P3 (when ready)

### 7. Skill Marketplace (brainstormed May 16)
Agents publish + verify skills. Late-stage feature.

### 8. Agent Collaboration OS (brainstormed May 16)
Task Forces, Agent Directory, Memory Anchors.

### 9. GUI / Tauri (Phase 6)
Web companion dashboard, not TUI mirror.

---

## Suggested next session

1. **Monitor Tab dashboard** — biggest visual upgrade, defined spec in DESIGN_SYSTEM
2. **Footer upgrade** — quick win, completes the 3-segment footer
3. If time: start on **State-Machine Workflows**

All three keep us within the DESIGN_SYSTEM spec and existing architecture.
